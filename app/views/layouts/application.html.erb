<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Live Chat" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="manifest" href="/manifest.json">
    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>

    <div id="banner" style="background-color: #4CAF50; color: white; text-align: center;">
      <p>current user:<%= current_user ? current_user.username : "Not logged in" %></p>
      <%= turbo_stream_from "chatrooms" %>
      <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
    </div>
    
    <%= yield %>
<%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>

  </body>
<%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>


</html>
<%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
